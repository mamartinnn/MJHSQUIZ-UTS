<!DOCTYPE html>
<html lang="en">
<head>

    <link rel="stylesheet" href="/Style/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MJHSQUIZZ</title>
    
</head>
<body>
    <div class="container">
        <form action="/" method="POST" class="m-8 p-8 form" autocomplete="off">
            <h1 class="text-center mb-4">Sign in</h1>

            <div class="mb-4">
                <label for="username" class="form-text pb-1">username</label>
                <input type="text" id="username" class="form-control" name="username" placeholder="username"
                    required>
            </div>

            <div class="mb-2">
                <label for="password" class="form-text pb-1">Password</label>
                <input type="password" id="password" class="form-control" name="password" placeholder="password"
                    required>
            </div>

            <a class="link fs-small d-block text-right" href="/signup">Create a new account</a>
                
            <div class="d-flex justify-content-end mt-8">
                <button type="reset" class="btn d-block ms-2">Clear</button>
                <button type="submit" class="btn btn-primary d-block ms-2">Login</button>
            </div>
        </form>
    </div>
    <% if (locals.success && success.length > 0) { %>
        <div style="color: green;"><%= success %></div>
    <% } %>
    <% if (locals.error && error.length > 0) { %>
        <div style="color: red;"><%= error %></div>
    <% } %>
    
</body>
</html>